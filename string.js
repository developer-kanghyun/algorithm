// 정수 n이 주어질 때, n을 문자열로 변환하여 return하도록 solution 함수를 완성해주세요.

const solution = String;

const solution1 = (n) => String(n);

const solution2 = (n) => n.toString();

const solution3 = (n) => n + "";

console.log(solution2(123));
